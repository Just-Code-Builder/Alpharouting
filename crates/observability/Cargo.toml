[package]
name = "observability"
version = "0.1.0"
edition.workspace = true

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["serde", "clock"] }
# Pinned exactly — see workspace root Cargo.toml for why.
serde = { version = "=1.0.220", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"
tracing = "0.1"
